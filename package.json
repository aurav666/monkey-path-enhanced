{
  "name": "monkey-path",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve . -s",
    "build": "echo \"Build step (if you have a bundler), otherwise copy HTML to dist/\"",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
